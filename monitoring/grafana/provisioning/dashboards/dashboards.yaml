apiVersion: 1
providers:
  - name: TrashTrack
    orgId: 1
    folder: TrashTrack
    folderUid: trashtrack
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
